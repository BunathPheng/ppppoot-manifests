---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ppppoot                  # Ingress name
  labels:
    app: ppppoot
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "ppppoot.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ppppoot               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
